apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  type: LoadBalancer
  selector:
    app: auth
  ports:
    - port: 80        # puerto externo (el que recibirá el balanceador)
      targetPort: 8000 # puerto dentro del contenedor
